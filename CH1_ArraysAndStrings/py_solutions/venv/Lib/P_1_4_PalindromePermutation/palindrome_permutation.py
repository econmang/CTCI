import sys
from itertools import permutations
 
'''
def isPalindromePerm(check_str: str)-> tuple:
    string = "stack"
    perms_all = [''.join(p) for p in permutations(string)]
    perms = [''.join(p) for p in permutations(string) if check_perm(check_str, ''.join(p)) ]
    print(len(perms_all))
    print(len(perms))
    return((True, []))

if __name__ == "__main__":
    isPalindromePerm("STR")
'''
